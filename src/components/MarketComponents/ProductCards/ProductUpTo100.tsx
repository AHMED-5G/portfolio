import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const ProductUpTo100 = (props: Props) => {
  return (
    <View>
      <Text>ProductUpTo100</Text>
    </View>
  );
};

export default ProductUpTo100;

const styles = StyleSheet.create({});
